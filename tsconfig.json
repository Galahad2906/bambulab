{
  "compilerOptions": {
    "target": "ESNext",                           // Permite usar las últimas características de JS
    "useDefineForClassFields": true,              // Compatible con la nueva forma de declarar campos
    "module": "ESNext",                           // Módulos ES modernos
    "moduleResolution": "Node",                   // Resolución de módulos estilo Node.js
    "strict": true,                               // Activa el modo estricto de TypeScript
    "jsx": "react-jsx",                           // Transformador JSX moderno para React 17+
    "esModuleInterop": true,                      // Permite importaciones por default desde CommonJS
    "skipLibCheck": true,                         // Omite verificación de tipos en libs externas
    "forceConsistentCasingInFileNames": true,     // Evita errores por diferencias de mayúsculas/minúsculas
    "baseUrl": "src"                              // ✅ Permite imports absolutos desde la carpeta src
  },
  "include": ["src"]                              // Analiza solo los archivos fuente del proyecto
}
