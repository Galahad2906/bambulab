const Loader = () => {
  return (
    <div className="fixed inset-0 bg-bambu text-white flex items-center justify-center z-50">
      <div className="animate-pulse text-3xl font-bold tracking-widest">
        BAMBULAB
      </div>
    </div>
  );
};

export default Loader;
